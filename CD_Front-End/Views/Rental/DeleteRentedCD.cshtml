@model CD_Front_End.Models.RentalItem

@{
    ViewBag.Title = "Delete Rented CD";
}

<h2>Delete Rented CD</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Rental Item</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.RentalID)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.RentalID)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.cdID)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CDs.Where(m => m.Value.Equals(model.cdID.ToString())).Single().Text)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

<div class="form-actions no-color">
    <input type="submit" value="Delete" class="btn btn-default" /> |
    @Html.ActionLink("Back to Rental", "Edit", new { id = Model.RentalID })
</div>
    }
</div>
